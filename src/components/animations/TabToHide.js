import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
gsap.registerPlugin(ScrollTrigger);

export const TabToHide = (ref) => {
  //   const el = ref.current;
  //   el.addEventListener("click", () => {
  //     gsap.to(el, {
  //       ease: "power2.out",
  //       duration: 1,
  //       yPercent: -60,
  //       xPercent: -40,
  //       inertia: true,
  //       rotate: 30,
  //     });
  //   });
};
